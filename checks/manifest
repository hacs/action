#!/bin/bash
source "$GITHUB_ACTION_PATH/checks/common"

URL="https://hacs.xyz/docs/publish/include#check-manifest"
GROUP="manifest"


checkInit "$GROUP" "$URL"

checkSuccess test

checkError test
checkError test $URL

checkComplete